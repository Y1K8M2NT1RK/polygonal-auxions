# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_DB=db
DATABASE_USER=root
DATABASE_PASSWORD=pswd
DATABASE_HOST=db
DATABASE_PORT=5433

TZ=Asia/Tokyo
POSTGRES_DB=$DATABASE_DB
POSTGRES_USER=$DATABASE_USER
POSTGRES_PASSWORD=$DATABASE_PASSWORD
POSTGRES_HOSTNAME=$DATABASE_HOST
PGDATA=/var/lib/postgresql/data/pgdata

JWT_SECRET=secret_key
JWT_REFRESH_SECRET=refresh_secret_key

# Email / App Configuration
APP_ENV=development
EMAIL_PROVIDER=smtp
RESEND_API_KEY=
RESEND_FROM=notifications@polygonal-auxions.local
SMTP_HOST=mailpit
SMTP_PORT=1025
ALLOWED_EMAIL_DOMAINS=polygonal-auxions.local,example.com

# Base URL used for building links in emails (no trailing slash)
# If running locally via docker compose exposing app on 3001 -> set port accordingly.
APP_HOST=localhost
APP_PORT=3001
APP_BASE_URL=http://localhost:3001

DATABASE_URL="postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:5432/${DATABASE_DB}?schema=public"
query Artwork($slug_id: String!) {
    artwork(slug_id: $slug_id) {
        slug_id
        title
        likes
        bads
        feature
        created_at
        user {
            handle_name
        }
        comments {
            body
            created_at
            user {
                handle_name
            }
        }
    }
}